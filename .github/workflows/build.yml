name: Dopamine Flutter unsigned IPA

on:
  push:
    branches: ['**']
    paths-ignore: ['**.md']
  pull_request:
  workflow_dispatch:

concurrency:
  group: build-${{ github.ref }}
  cancel-in-progress: true

jobs:
  ios:
    runs-on: macos-15
    timeout-minutes: 150
    steps:
      - uses: actions/checkout@v4
        with:
          submodules: recursive
          fetch-depth: 0

      - name: Select Xcode
        run: |
          set -x
          if [ -d /Applications/Xcode_16.4.app ]; then
            sudo xcode-select --switch /Applications/Xcode_16.4.app
          else
            sudo xcode-select --switch /Applications/Xcode.app
          fi

      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true

      - name: Initialize Flutter iOS host project
        run: |
          set -euo pipefail
          flutter config --no-enable-android

          # Generate the ephemeral .ios/ host for a Flutter module.
          # Use a temp dir to avoid overwriting existing files.
          TMPDIR=$(mktemp -d)
          flutter create --template=module \
            --org=com.opa334 \
            --project-name=dopamine \
            "$TMPDIR/dopamine"

          # Copy only the generated host directory into our repo.
          rm -rf .ios
          cp -a "$TMPDIR/dopamine/.ios" .ios/

          # Restore our pubspec (flutter create may have modified it in temp).
          flutter pub get

      - name: Analyze Dart
        run: dart analyze --fatal-infos

      - name: Build Flutter iOS AOT framework
        run: |
          set -euo pipefail
          flutter build ios-framework \
            --no-debug --no-profile --no-codesign \
            --output=Application/Frameworks/FlutterProducts

      - name: Install Procursus and ldid
        uses: dhinakg/procursus-action@main
        with:
          packages: ldid make sed coreutils findutils

      - name: Download bootstraps
        working-directory: Application/Dopamine/Resources
        run: ./download_bootstraps.sh

      - name: Build Packages (deb files)
        run: |
          set -euo pipefail
          cd Packages
          make -C libkrw-provider all package
          make -C libroot all package
          make -C basebin-link package || true

      - name: Prepare BaseBin include headers
        run: |
          set -euo pipefail
          brew install libarchive
          export CFLAGS="-I$(brew --prefix libarchive)/include"
          export LDFLAGS="-L$(brew --prefix libarchive)/lib"
          cd BaseBin
          rm -rf .include
          mkdir -p .include
          cp -r _external/include/* .include
          mkdir -p .include/libjailbreak
          cp libjailbreak/src/*.h .include/libjailbreak/
          mkdir -p .include/xpf
          cp XPF/src/xpf.h .include/xpf/
          # Remove bundled XPC headers if the SDK already provides them (iOS 17.4+).
          SDK_XPC="$(xcrun --sdk iphoneos --show-sdk-path)/usr/include/xpc.modulemap"
          if [ -f "$SDK_XPC" ]; then
            rm -rf .include/xpc
          fi

      - name: Build libjailbreak
        run: |
          set -euo pipefail
          brew install libarchive
          export CFLAGS="-I$(brew --prefix libarchive)/include"
          export LDFLAGS="-L$(brew --prefix libarchive)/lib"
          cd BaseBin
          mkdir -p .build
          cp -r _external/basebin/* .build
          cp _external/basebin/.version .build
          make -C ChOma TARGET=ios DISABLE_SIGNING=1 DISABLE_TESTS=1 DYLIB_LDFLAGS="-install_name @loader_path/libchoma.dylib -framework CoreFoundation"
          cp ChOma/output/ios/lib/libchoma.dylib .build
          mkdir -p .include/choma
          cp -r ChOma/output/ios/include/choma/* .include/choma/
          make -C libjailbreak
          cp libjailbreak/libjailbreak.dylib .build/

      - name: List Flutter framework output
        run: find Application/Frameworks -name '*.xcframework' | head -20

      - name: Copy Flutter framework to standard location
        run: |
          mkdir -p Application/Frameworks/FlutterEngine
          cp -a Application/Frameworks/FlutterProducts/Release/Flutter.xcframework/ios-arm64/Flutter.framework Application/Frameworks/FlutterEngine/

      - name: Build native application with embedded Flutter framework
        working-directory: Application
        env:
          COMMIT_HASH: ${{ github.sha }}
        run: |
          set -euo pipefail
          xcodebuild -project Dopamine.xcodeproj \
            -scheme Dopamine \
            -configuration Release \
            -derivedDataPath build \
            -destination 'generic/platform=iOS' \
            CODE_SIGNING_ALLOWED=NO \
            CODE_SIGN_IDENTITY='' \
            CODE_SIGN_ENTITLEMENTS='' \
            COMPILER_INDEX_STORE_ENABLE=NO \
            GCC_PREPROCESSOR_DEFINITIONS='NIGHTLY=1 COMMIT_HASH=\"${COMMIT_HASH}\"' \
            FRAMEWORK_SEARCH_PATHS='$(inherited) "$(SRCROOT)/Frameworks/FlutterEngine"' \
            OTHER_LDFLAGS='$(inherited) -framework Flutter -F $(SRCROOT)/Frameworks/FlutterProducts/Release' \
            build | xcbeautify

      - name: Embed Flutter framework and create unsigned IPA
        run: |
          set -euo pipefail
          APP="Application/build/Build/Products/Release-iphoneos/Dopamine.app"
          ENTITLEMENTS="Application/Dopamine/Dopamine.entitlements"
          test -d "$APP"

          mkdir -p "$APP/Frameworks"
          mkdir -p "$APP/Frameworks"`n          cp -a "Application/Frameworks/FlutterEngine/Flutter.framework" "$APP/Frameworks/"
          ldid -S"$ENTITLEMENTS" "$APP/Dopamine"

          rm -rf Payload Dopamine.ipa
          mkdir Payload
          cp -a "$APP" Payload/Dopamine.app
          zip -qry Dopamine.ipa Payload
          rm -rf Payload

      - name: Upload unsigned IPA
        uses: actions/upload-artifact@v4
        with:
          name: Dopamine-Flutter-${{ github.sha }}
          path: Dopamine.ipa
          if-no-files-found: error

















